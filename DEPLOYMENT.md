# النشر عبر Docker | Deployment

نشر بصير - عيون المالك كحاوية Docker على خادم بعيد.

> ⚠️ **تنبيه أمني**: `deploy.sh` يحتوي كلمة مرور خادم SSH بنص صريح (`root@13.140.138.252`)، بناءً على طلب صريح. تم إدراج `deploy.sh` و `*.tar` في `.gitignore` حتى لا تُرفَع لأي مستودع Git. **البديل الأصوب لاحقاً**: التحول لمصادقة بمفتاح SSH (`ssh-copy-id`) وحذف كلمة المرور من الملف نهائياً — راجع قسم [تحسين الأمان](#تحسين-الأمان-لاحقاً) بالأسفل.

## ⚠️ هذا خادم مشترك — يستضيف مشاريع أخرى

الخادم `13.140.138.252` (المضيف `vmi3351154`) يشغّل عدة مشاريع أخرى بجانب بصير (aqar-mudar, bevip-app, bassir-erp-app, social-media-system, ...). لذلك:

- كل أوامر `docker` في `run.sh`/`deploy.sh` مقيّدة بالاسم `bassir-owner-eyes` فقط (حاوية وصورة) — **لا تلمس أي مشروع آخر أبداً**.
- **المنفذ يُختار تلقائياً**: `run.sh` لا يفترض 3000 — يتحقق إن كان مشغولاً (من مشروع آخر) ويجرّب 3001 ثم 3002... حتى يجد منفذاً فارغاً، ويحفظه في `.port` بجانبه على الخادم لإعادة استخدامه بالتحديثات القادمة.
- **الحاوية مكشوفة مباشرة على الإنترنت** على المنفذ المختار (`0.0.0.0:<port>`) — قرار مقصود لسرعة الوصول الآن (`http://13.140.138.252:<port>`) بلا حاجة لدومين أو Nginx. تأكد أن المنفذ مفتوح في جدار الحماية/Security Group. لاحقاً، للحصول على دومين و HTTPS، يمكن وضع Nginx (الموجود مسبقاً على هذا الخادم — راجع `nginx-backups/`) أمامها كـ Reverse Proxy، وعندها يُفضَّل إعادة تقييد الحاوية لـ `127.0.0.1` فقط (`-p "127.0.0.1:${HOST_PORT}:3000"` في `run.sh`) لمنع الوصول المباشر للمنفذ بجانب الدومين.
- المجلد `/root/bassir-owner-eyes` **تم إنشاؤه بالفعل يدوياً** على الخادم — `deploy.sh` سينشئه فقط إن لم يكن موجوداً (لن يحذف شيئاً بداخله).

## المتطلبات

- **محلياً (جهازك)**: Docker Desktop يعمل، و Git Bash أو أي bash shell (الملفات `.sh`).
- **على الخادم**: Docker مثبَّت ويعمل (`docker --version`)، ومجلد `bassir-owner-eyes` موجود (تم إنشاؤه بالفعل).
- **(اختياري لتشغيل آلي كامل بلا طلب كلمة مرور)**: `sshpass` مثبتة محلياً:
  - Windows (Git Bash): `choco install sshpass` أو نزّل نسخة مجمّعة يدوياً
  - macOS: `brew install hudochenkov/sshpass/sshpass`
  - Linux: `sudo apt-get install sshpass`

  بدونها، ستُطلب كلمة المرور يدوياً عدة مرات أثناء التشغيل — يعمل بنفس الطريقة، فقط أقل أتمتة.

## الملفات

| الملف | الغرض |
|---|---|
| `Dockerfile` | يبني صورة الخادم (Node 22 + SQLite مدمجة، بلا اعتماديات بناء) |
| `.dockerignore` | يستثني `node_modules`, `data/`, `.env`, الخ من سياق البناء |
| `deploy.sh` | **يعمل محلياً**: يحذف الصورة القديمة، يبني صورة جديدة، يحفظها `.tar`، يرفعها + `run.sh` للخادم عبر `scp`، ثم يشغّل `run.sh` هناك عبر `ssh` |
| `run.sh` | **يعمل على الخادم** (يُرفَع تلقائياً): يوقف الحاوية القديمة، يحذف الصورة القديمة، يحمّل الصورة الجديدة من الـ tar، ويشغّل حاوية جديدة مع Volume دائم للبيانات |

## النشر بأمر واحد

من جذر المشروع، على جهازك:

```bash
chmod +x deploy.sh run.sh
./deploy.sh
```

هذا ينفّذ بالترتيب:
1. حذف الصورة المحلية القديمة `bassir-owner-eyes:latest` (إن وُجدت)
2. `docker build` لصورة جديدة من `Dockerfile`
3. `docker save` لحفظها كملف `bassir-owner-eyes.tar`
4. إنشاء المجلد `/root/bassir-owner-eyes` على الخادم (إن لم يوجد)
5. رفع `bassir-owner-eyes.tar` و `run.sh` إليه عبر `scp`
6. تشغيل `run.sh` على الخادم عبر `ssh`، والذي بدوره:
   - يحذف الحاوية القديمة `bassir-owner-eyes` (إن كانت تعمل)
   - يحذف الصورة القديمة على الخادم
   - `docker load` للصورة الجديدة من الـ tar
   - `docker run` لحاوية جديدة، بمنفذ `3000` ومجلد بيانات دائم (Volume) خارج الحاوية
7. حذف ملف الـ tar المحلي (تنظيف)

عند الانتهاء، السكربت يطبع المنفذ الفعلي المختار (مثلاً `✅ الحاوية bassir-owner-eyes تعمل على المنفذ 3001`). الحاوية تستمع محلياً على الخادم فقط (`127.0.0.1:<port>`) — الوصول من الإنترنت يتم عبر Nginx (راجع [القسم أعلاه](#️-هذا-خادم-مشترك--يستضيف-مشاريع-أخرى)) بربط دومين/مسار فرعي بهذا المنفذ.

## التشغيل اليدوي خطوة بخطوة (بديل)

إن رغبت بتنفيذ كل خطوة يدوياً بدل `deploy.sh`:

```bash
# 1) حذف الصورة المحلية القديمة
docker rmi -f bassir-owner-eyes:latest

# 2) بناء صورة جديدة
docker build -t bassir-owner-eyes:latest .

# 3) حفظها كملف tar
docker save -o bassir-owner-eyes.tar bassir-owner-eyes:latest

# 4) رفعها للخادم (سيطلب كلمة المرور: qmajdna@12pdpsa)
scp bassir-owner-eyes.tar run.sh root@13.140.138.252:/root/bassir-owner-eyes/

# 5) الدخول للخادم وتشغيل السكربت
ssh root@13.140.138.252
cd /root/bassir-owner-eyes
chmod +x run.sh
./run.sh
```

أو نفّذ ما يقابل `run.sh` يدوياً على الخادم مباشرة:

```bash
docker rm -f bassir-owner-eyes 2>/dev/null || true
docker rmi -f bassir-owner-eyes:latest 2>/dev/null || true
docker load -i bassir-owner-eyes.tar
mkdir -p data
# تحقق أولاً أن المنفذ فارغ (خادم مشترك — مشاريع أخرى قد تستخدم 3000):  ss -ltn | grep 3000
docker run -d \
  --name bassir-owner-eyes \
  --restart unless-stopped \
  -p 127.0.0.1:3000:3000 \
  -v "$(pwd)/data:/app/data" \
  --env-file .env \
  bassir-owner-eyes:latest
```

(غيّر `3000` لأي منفذ فارغ إن كان 3000 مشغولاً من مشروع آخر على هذا الخادم.)

## البيانات الدائمة (SQLite + الملفات المرفوعة)

قاعدة البيانات (`data/bassir.db`) والملفات المرفوعة (`data/uploads/`) تُخزَّن داخل الحاوية في `/app/data`. `run.sh` يربطها بمجلد `data/` **بجانب** `run.sh` على الخادم (Bind Mount)، بحيث:
- تبقى البيانات محفوظة حتى عند حذف/استبدال الحاوية أو الصورة
- يمكن أخذ نسخة احتياطية بنسخ `data/bassir.db` مباشرة من الخادم

## تفعيل الخدمات الحقيقية (اختياري)

انسخ `.env.example` إلى `.env` **على الخادم نفسه** (داخل `/root/bassir-owner-eyes/.env`)، واملأ ما تحتاجه (`ANTHROPIC_API_KEY`, `RESEND_API_KEY`, `WHATSAPP_TOKEN`, ...). `run.sh` يمرره تلقائياً للحاوية عبر `--env-file` إن وُجد. أي متغير غير مهيأ يعمل بوضع المحاكاة تلقائياً — لا حاجة لإعادة بناء الصورة عند تعديل `.env`، فقط أعد تشغيل الحاوية:

```bash
docker restart bassir-owner-eyes
```

## أوامر مفيدة على الخادم

```bash
docker ps                              # التحقق أن الحاوية تعمل
docker logs -f bassir-owner-eyes       # متابعة السجل الحي
docker restart bassir-owner-eyes       # إعادة تشغيل (بعد تعديل .env مثلاً)
docker exec -it bassir-owner-eyes sh   # الدخول داخل الحاوية
```

## تحديثات لاحقة

في كل مرة تُعدَّل فيها الشيفرة وتريد نشرها من جديد، فقط شغّل مرة أخرى من جهازك:

```bash
./deploy.sh
```

يكرر نفس الدورة: حذف الصورة القديمة → بناء جديدة → رفع → تشغيل، مع الحفاظ على بيانات `data/` كما هي على الخادم.

## تحسين الأمان لاحقاً

عند الاستقرار، يُنصح بشدة بـ:
1. **مصادقة بمفتاح SSH بدل كلمة مرور**: `ssh-copy-id root@13.140.138.252` ثم حذف `REMOTE_PASS` من `deploy.sh` (السكربت سيعمل بدون تعديل إضافي — `ssh`/`scp` تستخدم المفتاح تلقائياً إن وُجد ولم تُستخدم `sshpass`).
2. **تغيير كلمة مرور الخادم** بما أنها ظهرت بنص صريح في هذه المحادثة وفي `deploy.sh`.
3. **مستخدم غير root للنشر** بدل الدخول المباشر بـ root.
4. **جدار حماية / Reverse Proxy (nginx + HTTPS)** أمام المنفذ 3000 بدل تعريضه مباشرة على الإنترنت.
